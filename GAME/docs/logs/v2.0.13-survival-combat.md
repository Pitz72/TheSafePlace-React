# v2.0.13 — Sopravvivenza & combattimento: danno reale, cure complete, ricompense

**Data**: 2026-07-12
**Contesto**: blocco 6 della campagna di chiusura.

## Sopravvivenza (`characterStore`)
- **Danno da fame/sete/status finalmente reale in esplorazione**: il danno per tick era `Math.floor` sul singolo step da 10 minuti → fame a 0 = `floor(0.33)` = 0 HP, per sempre. Ora il **resto frazionario si accumula** (`survivalDamageDebt`, modulo-level, reset a ogni `initCharacter`): l'HP resta intero ma il decadimento morde davvero. I messaggi journal degli status compaiono solo quando il danno viene applicato (prima dichiaravano "-1 HP" mentre l'HP era intatto).
- **Tutti gli status ora curabili**: `MED_ANTISEPTIC` cura INFEZIONE, `medkit_advanced` cura anche MALATO (dati public). Già esistenti: antidoto→AVVELENATO, antidolorifico/medkit→FERITO, erbe→INFEZIONE. (IPOTERMIA resta contenuto latente: niente lo applica.)
- **XP: tabella cumulativa usata come incremento** → il grind di metà partita era ~raddoppiato (liv. 5 costava 3450 XP invece di 2000). `applyLevelUp` ora usa il **delta** tra soglie consecutive.
- **Potenziamenti armatura di Anya finalmente reali**: (a) il binding Ink `upgradeArmor` era uno stub `console.warn` → ora chiama `upgradeEquippedArmor`; (b) `getPlayerAC` non leggeva i flag `ARMOR_UPGRADED_<SLOT>_<BONUS>` → ora somma il bonus quando lo slot è equipaggiato e integro.

## Combattimento (`combatStore`)
- **Ricompense estratte in `grantVictoryRewards()`** (XP, loot, contatore vittorie, kill count taglie, cutscene primo kill umano) e chiamate da OGNI percorso di uccisione: **la morte per munizioni incendiarie dava vittoria a zero ricompense**.
- **Loot fantasma**: le tabelle umanoidi droppavano 4 id inesistenti (`armor_makeshift_helmet`, `armor_durable_boots`, `armor_leather_chestpiece`, `tactical_helmet`) → rimappati su armature reali (`armor_rags`, `tattered_pants`, `leather_jacket`, `tactical_leg_armor`).
- **Danno negativo**: arma debole + modificatore negativo poteva *curare* il nemico (`max(0, hp-(-1))`). Clamp a minimo 1 (attacco e imboscata Guerrigliero).
- **Armi da lancio**: usate dal menu oggetti venivano consumate senza alcun effetto → ora infliggono `danno + mod DES` (min 1).
- **Caricamento munizioni fallito** (munizioni assenti) non consuma più il turno.

## Verifica
- `tsc`: nessun errore nuovo nei file toccati (restano TS6133 preesistenti su import inutilizzati). `npm run build` ✅. JSON validati.
