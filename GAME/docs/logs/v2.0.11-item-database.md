# v2.0.11 — Database item completo: file non caricati, doppioni, id rotti

**Data**: 2026-07-12
**Contesto**: blocco 4 della campagna di chiusura.

## Fix

### `src/data/itemDatabase.ts`
- Aggiunti al loader **`repair_kits.json`** e **`unique_items.json`**: erano shippati ma mai caricati → 8 item non esistevano a runtime. Il caso peggiore: `tool_repair_kit_basic` è l'output di una **ricetta iniziale** (`recipe_repair_kit_basic`) — il crafting consumava gli ingredienti, mostrava "Hai creato…" e non dava nulla (`addItem` no-op su id sconosciuto). Idem `tool_repair_kit_advanced` (ricetta insegnata da Anya via Ink + ricompensa quest `find_elara`).
- Fix import `'../../types'` → `'../types'` (path errato preesistente, mascherato dall'elisione type-only di esbuild; rompeva il typecheck).

### `public/data/items/unique_items.json`
- Rimossi i **doppioni** `tech_components` e `titanic_cassette`: già definiti in `materials.json`/`restored_items.json` (versioni canoniche live). Prima della dedup, l'ordine di caricamento avrebbe silenziosamente sovrascritto valori/rarità.
- Restano 6 item unici ora effettivamente ottenibili: `broken_drone`, `advanced_tool_kit`, `vintage_film_reel`, `vinyl_record_moon`, `energy_drink`, `electronic_parts` (tutte le ricompense donor riparate in v2.0.9 ora esistono davvero nel DB).

### `quests.json` (public + src)
- Ricompense taglie con id inesistenti (perse in silenzio): `AMMO_12G` → `ammo_shotgun` (bounty cinghiali), `AMMO_9MM` → `ammo_9mm` (bounty razziatori).

## Non toccato (deliberato)
- `drone_memory_chip` duplicato in `quest.json`/`restored_items.json`: comportamento live invariato da sempre (vince restored_items), nessun impatto sulla quest (`hasItems` controlla solo l'id).
- Le 9 ricette con output fantasma (`weapon_molotov`, ecc.): non raggiungibili (nessun manuale né nodo Ink le insegna — verificato `learnRecipe` in tutti i .ink: solo 3 chiamate, tutte a ricette valide). Contenuto latente, innocuo.

## Verifica
- `tsc` pulito sui file toccati (spariti anche i 4 errori preesistenti di itemDatabase), `npm run build` ✅, JSON validati.
