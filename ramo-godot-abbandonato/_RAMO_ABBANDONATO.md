# Ramo Godot — ABBANDONATO

Questa cartella è un **archivio** del porting di *The Safe Place* realizzato con **Godot Engine**.

- **Stato:** abbandonato / non mantenuto. Il ramo attivo e completo del gioco è la versione **React/Electron** nella root di questo repository.
- **Contenuto:** codice sorgente Godot completo (`project.godot`, `scripts/`, `scenes/`, `maps/`, `textures/`, `themes/`, `data/`, `tilesets/`), test e documentazione di sviluppo (`development_history/`, `diagrams/`, `user_docs/`, `_archive/`, `_legacy/`).
- **Escluso dall'archivio:** la cache dell'editor (`.godot/`, `.import/`), la history git originale (`.git/`) e i file temporanei. Aprendo il progetto in Godot la cache verrà rigenerata automaticamente.
- **Archiviato il:** 2026-07-12.

Conservato a scopo storico e di riferimento.
